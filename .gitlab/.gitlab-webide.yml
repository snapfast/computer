terminal:
  # This can be any image that has the necessary runtime environment for your project.
  image: golang:latest
  before_script:
    - apt update
  script: sleep 60
  variables:
    GOP_ENV: "test"
